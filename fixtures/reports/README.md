# Conformance report fixtures

This directory stores task-level conformance report fixtures used by `textconformance` and `textlab` tests.

The reports are executable oracle fixtures, not a support-label ledger. They must remain tied to package behavior, validator output, and limitation text inside each report.
