# Package release fixtures

This directory stores public package release gate requirements.

`gates.v1.json` keeps package release tracks, release-readiness state, release
blockers, downstream API stability requirements, gate coverage, evidence references, and limitations
machine-readable.
