# Performance fixtures

This directory stores public performance gate requirements.

`gates.v1.json` defines which throughput, memory, streaming, large-corpus, and regression-threshold
evidence must exist before broader operational claims are made. It does not persist wall-clock or
memory measurements.
