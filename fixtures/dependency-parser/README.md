# Dependency parser fixtures

These fixtures freeze expected dependency arcs for the current dependency-parser slice.

Current status:

- deterministic expected arcs for declared frozen slices;
- expected arcs are derived from repository-authored CoNLL-U fixture rows;
- broad Universal Dependencies support is not stated.

The next gate must broaden UD slices and performance thresholds before wider parser behavior.
