# Rule-backed NER fixtures

This fixture set records readiness artifacts for issue `#13`.

- `slices.json` freezes the entity-recognition slices that feature work must cover.
- `tool-versions.json` freezes comparator versions, the supported label policy, and the readiness
  scope.
- future `expected/` artifacts will use the schema already defined in
  `schemas/rule-backed-ner-expected-v1.schema.json`.

This readiness set does not yet claim implemented named entity behavior.
