# Corpus TF-IDF/BM25 fixtures

This fixture set records readiness artifacts for issue `#14`.

- `slices.json` freezes the explicit-token corpus, document order, query slices, and phenomena.
- `tool-versions.json` freezes formula names.
- `expected/` stores recorded canonical outputs for the declared formula set.

This readiness set does not require tokenization; every input is an explicit token list.
