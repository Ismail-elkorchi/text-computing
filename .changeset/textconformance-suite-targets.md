---
"@ismail-elkorchi/textconformance": minor
---

Add suite target declarations, target probe guards, target-backed suite reports,
the `run-suite` CLI path, and a consumer example for package fixtures, external
consumer evidence, and generated package artifacts.
