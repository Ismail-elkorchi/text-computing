# Changesets

This directory stores release change notes for package updates.

Use one changeset per externally visible package change. Do not add a changeset for documentation-only pull requests unless the documentation changes a published package contract.
