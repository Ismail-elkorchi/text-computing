# Noisy Text

`noisyTextQualityFindings` reports repeated characters, mixed alphanumeric tokens, spelling-variant candidates, OOV examples, and normalization candidates.

Lexical coverage uses caller-provided `textlex` wordlists or lexicons. No resource package is discovered by name.
