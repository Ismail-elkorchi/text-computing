# Corpus Quality

`analyzeCorpusQuality` accepts a structural corpus-compatible value with an id, document refs, metadata, and optional index metadata.

It reports duplicate document ids, required metadata gaps, partition imbalance, missing expected document refs, and aggregate document-quality risks. Root imports do not require `@ismail-elkorchi/textcorpus`.
