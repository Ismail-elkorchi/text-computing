# Style

`styleQualityFindings` runs caller-provided structural style rules with ids, kinds, messages, and patterns.

Rule ids are preserved in final evidence. The package does not bundle hidden grammar rules or require `textrules` at the root.
