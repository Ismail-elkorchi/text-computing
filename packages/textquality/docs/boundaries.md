# Boundaries

`textquality` reports diagnostics and processing-readiness candidates.

It does not silently repair text, normalize documents, correct OCR, train models, run pipelines, load datasets, perform search ranking, link entities, own corpus storage, import `textparallel`, or use neural/vector/remote services.
