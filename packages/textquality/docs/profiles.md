# Profiles

Quality profiles are caller-provided JSON-safe objects.

Profiles may declare enabled dimensions, expected scripts or languages, required corpus metadata keys, balance keys, thresholds, severity mappings, resource ids, rule ids, and metadata. The runtime does not discover profile packages.
