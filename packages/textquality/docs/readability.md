# Readability

`readabilityMetrics` and `lexicalDiversityMetrics` compute finite count-based metrics over an explicit document view.

Metrics include token counts, sentence and paragraph counts, words per sentence, average token length, average sentence length, type/token ratio, and hapax ratio. Missing denominators omit metrics instead of emitting non-finite numbers.
