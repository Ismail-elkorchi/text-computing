# @ismail-elkorchi/textpack-ar-kb

Generated ArabicKnowledgeBase recipe composite textpack.

```ts
import { loadArabicKnowledgeBase } from "@ismail-elkorchi/textpack-ar-kb";

const pack = await loadArabicKnowledgeBase();
```

## Required Components

- `@ismail-elkorchi/textpack-wordnet-ar`
- `@ismail-elkorchi/textpack-wikidata-ar`

## Optional Components

- None


## Publishability

Publishable: `false`
Status: `blocked`

- artifact-backed descriptors require local materialized payloads before publishability
- capability slot kb is artifact-backed without local task-usable payloads
- capability slot quality is artifact-backed without local task-usable payloads
- generated packs are non-publishable by default

