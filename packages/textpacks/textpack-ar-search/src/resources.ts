// Generated by tools/textpack-forge. Do not edit.

import { readFile } from "node:fs/promises";
import { gunzipSync } from "node:zlib";
import type { PackResourceMap } from "@ismail-elkorchi/textpack";

const resourceRoot = new URL("../resources/", import.meta.url);

async function readResource(
	resourcePath: string,
	encoding: "gzip-base64" | "utf8",
): Promise<string> {
	const text = await readFile(new URL(resourcePath, resourceRoot), "utf8");
	if (encoding === "utf8") return text;
	return gunzipSync(Buffer.from(text.trim(), "base64")).toString("utf8");
}

export async function resources(): Promise<PackResourceMap> {
	// biome-ignore format: generated resource map preserves deterministic resource ordering.
	return {
		"ar-search-normalization-policy": await readResource(
			"ar.search.normalization-policy.json",
			"utf8",
		),
		"ar-search-tokenization-hooks": await readResource(
			"ar.search.tokenization-hooks.tsv",
			"utf8",
		),
		"ar-search-morphology-hooks": await readResource(
			"ar.search.morphology-hooks.tsv",
			"utf8",
		),
		"ar-search-wordnet-synonyms": await readResource(
			"ar.search.wordnet-synonyms.tsv",
			"utf8",
		),
		"ar-search-profile": await readResource(
			"ar.search.profile.json",
			"utf8",
		),
		"ar-search-quality": await readResource(
			"ar.search.quality.json",
			"utf8",
		),
		"ar-search-quality-profile": await readResource(
			"ar.search.quality-profile.json",
			"utf8",
		),
	};
}
