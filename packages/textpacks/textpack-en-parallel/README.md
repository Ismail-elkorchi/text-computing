# @ismail-elkorchi/textpack-en-parallel

Generated parallel textpack.

This package is a generated data package. It exports structural textpack data only.
Use `@ismail-elkorchi/text-computing` for developer-facing NLP task APIs.
It is generated from pinned source snapshots by `tools/textpack-forge`.

```ts
import pack, { manifest, resources } from "@ismail-elkorchi/textpack-en-parallel";

console.log(manifest.packageName);
console.log(Object.keys(resources).length);
console.log(pack.manifest.resources.length);
```

## Resources

- `en-tatoeba-parallel-ara` (alignment-table, text/tab-separated-values)
- `en-tatoeba-parallel-deu` (alignment-table, text/tab-separated-values)
- `en-tatoeba-parallel-fra` (alignment-table, text/tab-separated-values)
- `en-tatoeba-parallel-spa` (alignment-table, text/tab-separated-values)
- `en-tatoeba-parallel-ita` (alignment-table, text/tab-separated-values)
- `en-tatoeba-parallel-ell` (alignment-table, text/tab-separated-values)
- `en-tatoeba-parallel-grc` (alignment-table, text/tab-separated-values)
- `en-tatoeba-parallel-lat` (alignment-table, text/tab-separated-values)
- `en-tatoeba-parallel-quality` (quality-profile, json)
- `en-tatoeba-parallel-quality-profile` (quality-profile, json)

## Publishability

Publishable: `true`
Status: `publishable`

- None

