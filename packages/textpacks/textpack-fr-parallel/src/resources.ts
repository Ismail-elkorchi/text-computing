// Generated by tools/textpack-forge. Do not edit.

import { readFile } from "node:fs/promises";
import { gunzipSync } from "node:zlib";
import type { PackResourceMap } from "@ismail-elkorchi/textpack";

const resourceRoot = new URL("../resources/", import.meta.url);

async function readResource(
	resourcePath: string,
	encoding: "gzip-base64" | "utf8",
): Promise<string> {
	const text = await readFile(new URL(resourcePath, resourceRoot), "utf8");
	if (encoding === "utf8") return text;
	return gunzipSync(Buffer.from(text.trim(), "base64")).toString("utf8");
}

export async function resources(): Promise<PackResourceMap> {
	// biome-ignore format: generated resource map preserves deterministic resource ordering.
	return {
		"fr-tatoeba-parallel-ara": await readResource(
			"tatoeba-fr.parallel-ara.tsv.gz.b64",
			"gzip-base64",
		),
		"fr-tatoeba-parallel-deu": await readResource(
			"tatoeba-fr.parallel-deu.tsv.gz.b64",
			"gzip-base64",
		),
		"fr-tatoeba-parallel-eng": await readResource(
			"tatoeba-fr.parallel-eng.tsv.gz.b64",
			"gzip-base64",
		),
		"fr-tatoeba-parallel-spa": await readResource(
			"tatoeba-fr.parallel-spa.tsv.gz.b64",
			"gzip-base64",
		),
		"fr-tatoeba-parallel-quality": await readResource(
			"tatoeba-fr.parallel-quality.json",
			"utf8",
		),
		"fr-tatoeba-parallel-quality-profile": await readResource(
			"tatoeba-fr.parallel-quality-profile.json",
			"utf8",
		),
	};
}
