# @ismail-elkorchi/textpack-ar-normalization

Generated language-concrete textpack.

This package is a generated data package. It exports structural textpack data only.
Use `@ismail-elkorchi/text-computing` for developer-facing NLP task APIs.
It is generated from pinned source snapshots by `tools/textpack-forge`.

```ts
import pack, { manifest, resources } from "@ismail-elkorchi/textpack-ar-normalization";

console.log(manifest.packageName);
console.log(Object.keys(resources).length);
console.log(pack.manifest.resources.length);
```

## Resources

- `ar-normalization-rules` (normalization-profile, tsv)
- `ar-normalization-observed-codepoints` (normalization-profile, tsv)
- `ar-normalization-profile` (normalization-profile, json)
- `ar-normalization-quality` (quality-profile, json)
- `ar-normalization-quality-profile` (quality-profile, json)

## Publishability

Publishable: `true`
Status: `publishable`

- None

