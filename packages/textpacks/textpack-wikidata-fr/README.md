# @ismail-elkorchi/textpack-wikidata-fr

Generated kb textpack.

This package is a generated data package. It exports structural textpack data only.
Use `@ismail-elkorchi/text-computing` for developer-facing NLP task APIs.
It is generated from pinned source snapshots by `tools/textpack-forge`.

```ts
import pack, { manifest, resources } from "@ismail-elkorchi/textpack-wikidata-fr";

console.log(manifest.packageName);
console.log(Object.keys(resources).length);
console.log(pack.manifest.resources.length);
```

## Resources

- `wikidata-fr-entities` (knowledge-base, text/tab-separated-values)
- `wikidata-fr-aliases` (knowledge-base, text/tab-separated-values)
- `wikidata-fr-relations` (knowledge-base, text/tab-separated-values)
- `wikidata-fr-kb-canonical` (knowledge-base, json)
- `wikidata-fr-quality` (quality-profile, json)
- `wikidata-fr-quality-profile` (quality-profile, json)

## Publishability

Publishable: `true`
Status: `publishable`

- None

