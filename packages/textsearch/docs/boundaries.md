# Boundaries

`textsearch` owns lexical retrieval, ranking, query parsing, filtering, faceting, highlighting, spelling suggestions, and explanations.

It does not own corpus linguistics, dataset loading, document normalization, model training, pipeline orchestration, entity linking, KB reasoning, quality diagnostics, translation memory, neural inference, vector search, package discovery, or filesystem index services.
