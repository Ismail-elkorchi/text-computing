# Highlight

`highlight` creates stable fragments from match spans.

Highlight spans must refer to final view text or indexed field text with UTF-16 code-unit offsets. Overlapping spans are merged before snippets are created. HTML escaping is enabled by default.
