# Filter

Filters can restrict results by document id, metadata value, field value, range, annotation, child query, and simple boolean combinations.

Filters operate over indexed or stored JSON-safe values. They do not read external data or inspect filesystem paths.
