# Patterns

Supported pattern kinds are `char`, `token`, `annotation`, `dependency`, `tree`, `sequence`, and
`boolean`. Pattern results carry spans, annotation ids, graph ids, edge ids, and named captures for
actions.
