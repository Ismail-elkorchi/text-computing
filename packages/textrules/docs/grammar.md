# Grammar

Grammar definitions cover local annotation grammars, token and annotation regular languages,
dependency and tree grammars, phrase grammars, feature constraints, agreement checks, and shallow
transfer declarations.
