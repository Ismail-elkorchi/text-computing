# Actions

Actions can create annotations, set features, rewrite views, split, merge, retokenize, create graph
edges, emit diagnostics, check constraints, and emit feature annotations. Existing document data is
preserved unless an action targets it.
