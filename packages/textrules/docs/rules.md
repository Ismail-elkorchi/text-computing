# Rules

Rule sets use `RuleSet`, `Rule`, `Pattern`, and `RuleAction`. `compileRuleSet` validates final rule
sets, assigns deterministic phase and priority defaults, and rejects unknown pattern or action kinds.
