# Processors

`createRuleProcessor` returns a structural processor with metadata and a `process` method. Built-in
processor factories wrap compiled rule sets for tokenization, sentence splitting, lemmatization,
stemming, chunking, entity recognition, relation extraction, event extraction, time extraction,
quantity extraction, citation extraction, coreference, quote attribution, style checking, and
transfer.
