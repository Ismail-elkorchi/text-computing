# Boundaries

`textrules` does not own statistical training, corpus storage, search indexes, KB reasoning,
normalization workflows, parallel alignment, or pipeline scheduling. It emits final annotations,
views, span maps, graph links, diagnostics, and features for other packages to consume.
