{
  "name": "@ismail-elkorchi/textpack-en-legal",
  "version": "0.1.0",
  "description": "pack:en-legal reference textpack resources.",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./pack.manifest.json": "./pack.manifest.json"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "check:pack": "npm pack --dry-run && node test/pack-audit.mjs",
    "test": "npm run -s build && node test/smoke.mjs && node test/negative.mjs && node test/representative.mjs && npm run -s check:pack",
    "prepack": "npm run build"
  },
  "dependencies": {
    "@ismail-elkorchi/textpack": "0.1.0"
  },
  "license": "MIT",
  "files": [
    "dist",
    "pack.manifest.json",
    "resources",
    "README.md",
    "CHANGELOG.md"
  ],
  "publishConfig": {
    "access": "public"
  }
}
