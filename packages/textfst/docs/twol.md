# Two-Level Rules

`compileTwol` accepts structured or textual lexical-to-surface pairs and compiles them into
transducers. It is an FST resource compiler only; annotation grammars and rule cascades belong to
`textrules`.
