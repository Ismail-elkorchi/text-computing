# Transducers

`buildTransducer` and `buildStringTransducer` create explicit input-to-output relations. `invert`
swaps the relation direction and `project` turns one side of a relation into an acceptor.
