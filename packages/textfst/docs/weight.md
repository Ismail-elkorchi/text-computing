# Weights

The package exposes boolean, tropical, and log semiring helpers. Weighted paths are ranked by
semiring order and deterministic string tie-breaks, and invalid non-finite weights are rejected.
