# Resources

Resource helpers accept caller-provided FST objects, serialized FST JSON, rewrite resources, twol
resources, morphology entries, or already loaded structural textpack values. Runtime code does not
read files, fetch URLs, or discover packs.
