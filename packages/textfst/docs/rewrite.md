# Rewrite Rules

`compileRewrite` compiles replacement rules into transducers. `rewriteText`, `transliterate`, and
`orthographicConvert` are explicit transducer application helpers and do not choose language
resources implicitly.
