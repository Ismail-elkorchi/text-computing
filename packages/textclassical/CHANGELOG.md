# Changelog

## 0.1.0

- Add the final `@ismail-elkorchi/textclassical` package surface.
- Add deterministic feature extraction, vectorization, classifiers, sequence taggers, n-gram
  language models, topic modeling, clustering, task wrappers, parser helpers, summaries, and
  statistical `textdoc` annotation helpers.
