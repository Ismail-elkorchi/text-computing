# CRF

The CRF trainer uses linear-chain features, transition weights, forward-backward expectations, and
deterministic update order. Convergence is controlled by explicit iteration and learning-rate
options.
