# Tagger

The tagger subpath contains task wrappers for POS, chunking, NER, slot filling, language
identification, and sentiment classification. Models and resources are explicit inputs; no hidden
pretrained model is loaded.
