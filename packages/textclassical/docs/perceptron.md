# Perceptron

Perceptron classifiers use deterministic sample order and stable label ordering. Averaged
perceptron stores averaged weights. The sequence perceptron uses linear-chain transition and token
features.
