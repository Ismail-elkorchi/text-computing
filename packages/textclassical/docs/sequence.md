# Sequence

`trainSequenceTagger` supports `hmm`, `memm`, `crf`, and `perceptron-sequence`.

`tagSequence` returns one label per token. `annotateSequence` exports final `textdoc` annotations
for POS, chunk, NER, slot, or custom sequence labels.
