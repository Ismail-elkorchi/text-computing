# Candidates

Candidates use the final section 12 kinds: `spelling`, `historical`, `ocr`, `dialect`,
`transliteration`, `punctuation`, `spacing`, and `casing`.

Every candidate includes a `SpanRef`, candidate string, final `Evidence`, and optional `Score`.
Ordering is deterministic by span, score, kind, candidate text, and resource identifiers.
