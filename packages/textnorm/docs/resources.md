# Resources

Resources are caller-owned values: spelling maps, historical maps, orthography maps, confusion
tables, transliteration maps, final `textlex` lexicons, final `textfst` FSTs, final `textrules`
rule sets, abbreviation tables, and already loaded structural resource-pack-shaped values.

Runtime code does not read package folders, local files, or the network.
