# Annotations

`annotateNormalization` creates final `view.*` annotation layers with evidence-bearing decision
annotations. Existing sources, views, span maps, layers, annotations, evidence, alternatives, and
graphs are preserved by returning a new document.
