# Variant Graphs

`buildVariantGraph` creates stable source and candidate nodes from generated or caller-provided
candidates. Edges retain candidate kind, evidence, and score so ambiguity can be audited without
discarding rejected alternatives.
