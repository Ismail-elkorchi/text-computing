# Historical Text

Historical normalization uses explicit historical spelling maps, orthography maps, abbreviation
tables, and witness or edition metadata. The diplomatic source view remains unchanged.

Historical view helpers map final modes to final `TextView.kind` values: editorial and orthographic
views use `historical-normalized`, search views use `search`, and lemma-oriented views use
`morphological`.
