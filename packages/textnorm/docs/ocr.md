# OCR And ATR

OCR/ATR mode accepts explicit character or word confusion tables, FST candidates, and line-break
hyphenation repair when the caller enables it. Confidence is optional; confidence-free diagnostic
workflows are supported by returning candidates and diagnostics without requiring confidence input.
