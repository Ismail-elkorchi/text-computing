# Collocation

`collocations` computes window-based co-occurrence around a structured query.

Supported association measures are `mi`, `mi3`, `t-score`, `z-score`, `chi-square`, `log-likelihood`, `dice`, `logdice`, `raw-frequency`, and `relative-frequency`.
