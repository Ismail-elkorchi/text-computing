# Boundaries

`textcorpus` owns corpus storage, structured corpus predicates, corpus statistics, corpus-side lexicography, feature profiles, repeated-passage detection, and time-based trend tables.

It does not read external data formats, train models, run ranked search engines, perform knowledge-base linking, or produce quality judgments. Those responsibilities belong to adjacent runtime packages.
