# Stylometry

`stylometricProfile`, `documentSimilarityMatrix`, and `lexicalDiversity` expose feature profiles only.

The package does not make final authorship claims or train authorship classifiers.
