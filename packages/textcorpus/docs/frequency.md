# Frequency

`frequency` and `wordList` compute token, lemma, or surface counts with document counts and relative frequencies.

`documentTermMatrix` returns per-document term rows for downstream corpus-side features.
