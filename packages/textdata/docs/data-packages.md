# Data Packages

Data-package manifests are explicit input metadata. They describe dataset identity, format, source descriptors, citations, split hints, and schema hints. The runtime does not scan directories, inspect package names, read paths, or fetch URLs from a manifest.
