# Formats

Supported read formats are `plain-text`, `jsonl`, `csv`, `tsv`, `conll`, `conllu`, `iob`, `tei`, `html`, `xml`, and `parallel`.

Supported write formats are `jsonl`, `textdoc-jsonl`, `csv`, `tsv`, `conllu`, `iob`, and `parallel`.
