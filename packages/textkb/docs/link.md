# Linking

`linkEntities`, `linkTerms`, and `disambiguateSense` return new `TextDocument` values.

Existing annotations, evidence, alternatives, graphs, views, span maps, and metadata are preserved. New annotations use final `kb` evidence and validated `utf16-code-unit` spans.
