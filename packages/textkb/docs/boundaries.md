# Boundaries

`textkb` stores and links knowledge. It does not crawl external KBs at runtime, train entity linkers, run neural inference, perform vector search, replace `textcorpus` terminology extraction, load datasets, orchestrate pipelines, or emit quality judgments.
