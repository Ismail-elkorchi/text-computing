# Ontology

Ontology support is relation-backed. Use `querySemanticRelations` and `traverseSemanticRelations` for instance, part, broader, narrower, equivalent, and domain-specific typed relations.

Use `ontologyGazetteer` or `annotateOntologyGazetteer` for KB-aware gazetteer matches filtered by ontology type/domain.
