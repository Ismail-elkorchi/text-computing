# Terms

Concept records represent termbase concepts. They can carry labels, aliases, definitions, domains, relations, priors, source mappings, and metadata.

Use `candidateConcepts` and `linkTerms` to connect term annotations or alias matches to concept ids.
