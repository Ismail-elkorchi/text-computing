# Senses

Sense records contain lemma, optional POS, language, definition, examples, aliases, priors, relations, and source mappings.

Use `candidateSenses` for candidate lookup and `disambiguateSense` to add final `sense.link` annotations to a `TextDocument`.
