# Semantic Relations

`textkb` supports synonymy, antonymy, hypernymy, hyponymy, meronymy, holonymy, instance-of, part-of, broader term, narrower term, related term, equivalent concept, and domain-specific typed relations.

Use `querySemanticRelations` for filtered lookup and `traverseSemanticRelations` for bounded deterministic traversal.
