# Resources

Resource parsers accept caller-provided strings, arrays, objects, or already loaded textpack resource values. Runtime code does not read files, fetch URLs, or discover packs. Pack adapters consume structural `{ manifest, resources }` values.
