# Tries And Indexes

The trie entrypoint exposes deterministic builders for tries, double-array tries, DAWGs, minimal perfect hash maps, prefix indexes, and suffix indexes. Builders sort and deduplicate keys so identical inputs produce stable output.
