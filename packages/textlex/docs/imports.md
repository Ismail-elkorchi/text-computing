# Imports

Use the root entrypoint for common APIs:

```ts
import { buildLexicon, lookup, annotateLexicon } from "@ismail-elkorchi/textlex";
```

Focused entrypoints are available for `lexicon`, `gazetteer`, `term`, `trie`, `phrase`, `fuzzy`, and `annotate`.
