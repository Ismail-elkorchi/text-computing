# Pronunciations

`buildPronunciationLexicon` stores written forms with listed pronunciations and notation metadata. `lookupPronunciations` returns stored values only and does not infer pronunciations for unseen forms.
