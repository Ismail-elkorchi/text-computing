# Abbreviations

`buildAbbreviationTable` stores abbreviation forms and listed expansions. `lookupAbbreviation` returns expansion facts only; sentence splitting and rule execution are outside `textlex`.
