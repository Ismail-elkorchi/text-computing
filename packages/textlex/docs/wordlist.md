# Wordlists And Stoplists

`buildWordlist` and `buildStoplist` create immutable membership resources. Membership checks use exact keys unless normalization or casefolding is declared by the caller or builder options.
