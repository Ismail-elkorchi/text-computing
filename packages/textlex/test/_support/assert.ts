import assert from "node:assert/strict";

export const assertEqual = assert.equal;
export const assertDeepEqual = assert.deepEqual;
export const assertOk = assert.ok;
export const assertThrows = assert.throws;
