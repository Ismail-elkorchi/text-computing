# Boundaries

`textparallel` owns parallel-document structures, alignment links, sentence/word alignment entrypoints, translation memory, bilingual candidate extraction, and shallow transfer composition.

It does not own dataset readers, generic corpus storage, lexicon construction, FST compilation, rule-language parsing, pipeline orchestration, or hidden translation resources.
