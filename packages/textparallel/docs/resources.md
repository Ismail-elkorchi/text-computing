# Resources

All resources are explicit inputs:

- bilingual dictionary entries
- `textlex` lexicons and stoplists
- `textfst` finite-state transducers
- `textrules` compiled rule sets
- existing translation-memory rows and aligned documents

The package does not bundle large corpora, phrase tables, dictionaries, or trained artifacts.
